//! Configuration types for persisted settings and coordinator runtime state.

mod ai;
mod coordinator;
mod settings;

pub use ai::AiConfig;
pub use coordinator::CoordinatorConfig;
pub use settings::ModelSettings;
