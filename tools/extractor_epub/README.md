# Epub Extractor

This tool extracts the contents of an Epub and converts it into our own JSON format that can be imported into Moretimer. It also extracts the cover image and saves it as a separate file.
