# NEXO Model Management

This crate provides Commands that can be used within the cli-helpers crate to add functionality for listing and pulling AI models for use in NEXO inference. It is used by the `nexo-node`, `nexo-gateway` and `nexo-ai` CLIs to offer the same functionality for the different CLI tools.
